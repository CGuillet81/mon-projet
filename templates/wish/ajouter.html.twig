{% extends 'front.html.twig' %}
{# on vient charger le template #}
{# AUCUN CODE HTML en dehors des block twig #}

{% block titre %} Ajouter - {{ parent() }} {% endblock %}
{# avec parent je recupere le contenu du bloque parent #}

{% block central%}

    <h1>Ajouter un souhait</h1>
    <div class="col-6">
        {{ form_start(formWish, {attr: {novalidate: 'novalidate'}}) }}
        {# <form action ="..." method="..." > #}

        {{ form_widget(formWish) }}
        {# lable + input  #}

        <button class="btn btn-primary" type="submit">Ajouter</button>

        {{ form_end(formWish) }}
    </div>
    {# </form> #}
{% endblock %}